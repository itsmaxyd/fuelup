# Signing Configuration for Fuelup App
# 1. Copy this file to 'key.properties' in the same directory (android/key.properties)
# 2. Ensure 'key.properties' is in your .gitignore (already configured)
# 3. Generate your keystore using the keytool command provided in the instructions
# 4. Update the passwords below to match what you set during generation

storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=upload
storeFile=upload-keystore.jks
